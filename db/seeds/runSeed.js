//this is a placeholder for the runSeed function - started but unfinished

// import mongoose from "mongoose";
// import dotenv from "dotenv";
// dotenv.config();

// const db = mongoose.connect;
// const local = process.env.DB_LOCAL;

// import seedDB from "./mongoSeed.js"

// const runSeed = () => {
//   return seedDB(devData).then(() => db.end());
// };

// runSeed();
